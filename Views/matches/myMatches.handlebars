
{{#each matches}}
<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{match.fullname}}</h5>
    <h6 class="card-subtitle mb-2 text-muted">{{match.age}} <br> {{match.about}}</h6>
    <a href="#" class="card-link">Card link</a>
  </div>
</div>

{{else}}
<h5>No matches yet!</h5>
{{/each}}